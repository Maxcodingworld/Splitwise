<h1>Dashboard :</h1>

<table>
  <tr>
    
    
    <th>&nbsp;&nbsp;Money_owe</th>
    <th>&nbsp;&nbsp;With</th>
    <th></th>
  </tr>

<% @microposts.each do |micropost| %>
  <tr>
   
    <td><%= micropost.amount %></td>
    <td><%= micropost.payer_id%></td>
    <td><%= link_to 'Show', micropost_path(micropost) %></td>
    <td><%= link_to 'Destroy', micropost_path(micropost), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Micropost', new_micropost_path %>
<%= link_to "Sign out", destroy_user_session_path %>
